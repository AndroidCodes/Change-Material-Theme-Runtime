<?xml version="1.0" encoding="utf-8"?>
<!-- @formatter:off -->
<manifest
    package="com.example.androidcodes.materialthemes"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.example.androidcodes.materialthemes.application.MaterialThemeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.example.androidcodes.materialthemes.controller.home.HomeActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
